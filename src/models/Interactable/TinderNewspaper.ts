import { Interactable } from './types';

const tinderNewspaper: Interactable = {
  hasBeenInteractedWith: false,
  name: 'Newspaper',
  type: 'tinder',
  variant: 'newspaper',
};

export default tinderNewspaper;
